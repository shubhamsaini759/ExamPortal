import { createSlice } from "@reduxjs/toolkit";

export const ExamStudents = createSlice({
            name : 'exam students',
            initialState : {
            
                studentID : ''
            },
            reducer : {

            }
})